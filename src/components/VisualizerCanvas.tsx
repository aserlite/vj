import { Canvas } from '@react-three/fiber';

export default function VisualizerCanvas() {
  return (
    <div className="visualizer-canvas-container h-screen w-full">
      <Canvas>
        {/* TODO: Setup SceneManager and components */}
      </Canvas>
    </div>
  );
}
